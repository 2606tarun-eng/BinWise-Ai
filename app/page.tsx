'use client'

import { useRef, useState } from 'react'
import {
  AlertTriangle,
  ArrowRight,
  Battery,
  LogOut,
  LockKeyhole,
  Mail,
  Camera,
  Check,
  ChevronRight,
  CircleHelp,
  UserRound,
  Clock3,
  CloudUpload,
  Droplets,
  ExternalLink,
  Flame,
  Footprints,
  Leaf,
  LocateFixed,
  Menu,
  PackageCheck,
  Recycle,
  Search,
  ShieldCheck,
  Sparkles,
  Trophy,
  Upload,
  X,
  Zap,
} from 'lucide-react'

type WasteKey = 'green' | 'blue' | 'red' | 'black'
type Waste = {
  label: string
  short: string
  material: string
  risk: number
  riskLabel: string
  riskColor: string
  chip: string
  icon: typeof Leaf
  current: string
  future: string
  process: string
  tip: string
}

const waste: Record<WasteKey, Waste> = {
  green: { label: 'Green Bin · Wet / Biodegradable', short: 'GREEN BIN', material: 'Kitchen & food waste', risk: 1, riskLabel: 'Safe', riskColor: 'emerald', chip: 'bg-emerald-50 text-emerald-700', icon: Leaf, current: 'Organic waste returns nutrients to the soil when separated correctly.', future: 'Composting avoids methane-heavy landfill decomposition.', process: 'Anaerobic digestion into biogas and nutrient-rich compost.', tip: 'Drain excess liquid, then add food scraps to your home compost caddy.' },
  blue: { label: 'Blue Bin · Dry / Recyclable', short: 'BLUE BIN', material: 'PET synthetic plastic', risk: 2, riskLabel: 'Moderate', riskColor: 'yellow', chip: 'bg-sky-50 text-sky-700', icon: Recycle, current: 'PET is valuable when clean, dry, and kept separate from wet waste.', future: 'Takes 450+ years to decompose in a landfill.', process: 'Shredded, washed, and remade into rPET flakes and fibre.', tip: 'Rinse and crush the bottle, then remove the cap before recycling.' },
  red: { label: 'Red Bin · Sanitary Waste', short: 'RED BIN', material: 'Soiled sanitary waste', risk: 3, riskLabel: 'High', riskColor: 'orange', chip: 'bg-red-50 text-red-700', icon: ShieldCheck, current: 'Soiled materials can carry pathogens and should never enter a recycling stream.', future: 'Improper disposal can contaminate soil and groundwater.', process: 'Controlled treatment and safe incineration at a certified facility.', tip: 'Wrap securely, label sanitary waste, and use a certified collection point.' },
  black: { label: 'Black Bin · Hazardous / E-waste', short: 'BLACK BIN', material: 'Lithium battery / e-waste', risk: 4, riskLabel: 'Critical', riskColor: 'red', chip: 'bg-slate-900 text-white', icon: Battery, current: 'Battery chemicals can leach into soil; damaged cells may ignite.', future: 'Heavy metals persist for centuries and can enter food systems.', process: 'TSDF extraction recovers metals under controlled conditions.', tip: 'Do not puncture or bin it. Tape terminals and take it to a certified hub.' },
}

const samples: { label: string; key: WasteKey; icon: typeof Leaf }[] = [
  { label: 'Kitchen waste', key: 'green', icon: Leaf },
  { label: 'Plastic bottle', key: 'blue', icon: Droplets },
  { label: 'Soiled bandage', key: 'red', icon: ShieldCheck },
  { label: 'Lithium battery', key: 'black', icon: Battery },
]

const harmTheme: Record<string, { card: string; label: string; body: string }> = {
  emerald: { card: 'border border-emerald-200 bg-emerald-50', label: 'text-emerald-700', body: 'text-emerald-900' },
  yellow: { card: 'border border-yellow-200 bg-yellow-50', label: 'text-yellow-700', body: 'text-yellow-900' },
  orange: { card: 'border border-orange-200 bg-orange-50', label: 'text-orange-700', body: 'text-orange-900' },
  red: { card: 'border border-red-200 bg-red-50', label: 'text-red-700', body: 'text-red-900' },
}

function SectionTitle({ icon: Icon, children }: { icon: typeof Leaf; children: React.ReactNode }) {
  return <div className="mb-4 flex items-center gap-2 text-[11px] font-bold uppercase tracking-[0.14em] text-slate-500"><Icon className="h-4 w-4 text-emerald-600" />{children}</div>
}

export default function Page() {
  const [drawer, setDrawer] = useState(false)
  const [activeTab, setActiveTab] = useState<'home' | 'leaderboard' | 'impact'>('home')
  const [input, setInput] = useState('')
  const [fileName, setFileName] = useState('')
  const [result, setResult] = useState<WasteKey>('blue')
  const [analyzing, setAnalyzing] = useState(false)
  const [analyzed, setAnalyzed] = useState(false)
  const [journey, setJourney] = useState(1)
  const [proofName, setProofName] = useState('')
  const [toast, setToast] = useState(false)
  const [dialog, setDialog] = useState(false)
  const [guide, setGuide] = useState<'industrial' | 'diy' | null>(null)
  const [diyProofName, setDiyProofName] = useState('')
  const [diySkipped, setDiySkipped] = useState(false)
  const [authOpen, setAuthOpen] = useState(false)
  const [authTab, setAuthTab] = useState<'login' | 'signup'>('login')
  const [profileOpen, setProfileOpen] = useState(false)
  const [loggedIn, setLoggedIn] = useState(false)
  const [userEmail, setUserEmail] = useState('alex@example.com')
  const [userName, setUserName] = useState('Alex Morgan')
  const [userXp, setUserXp] = useState(0)
  const [authError, setAuthError] = useState('')
  const fileRef = useRef<HTMLInputElement>(null)
  const item = waste[result]
  const ResultIcon = item.icon
  const harm = harmTheme[item.riskColor]

  function analyze() {
    if (!input.trim() && !fileName) return
    setAnalyzing(true)
    setTimeout(() => {
      const text = input.toLowerCase()
      const next: WasteKey = text.includes('battery') || text.includes('charger') || text.includes('lithium') ? 'black' : text.includes('bandage') || text.includes('sanitary') || text.includes('diaper') ? 'red' : text.includes('food') || text.includes('kitchen') || text.includes('peel') ? 'green' : 'blue'
      setResult(next)
      setAnalyzing(false)
      setAnalyzed(true)
      setJourney(1)
      document.getElementById('ai-classification-result')?.scrollIntoView({ behavior: 'smooth', block: 'start' })
    }, 1500)
  }

  function dropped() {
    setJourney(3)
    if (proofName) setUserXp(20)
    setToast(true)
    setTimeout(() => setToast(false), 2800)
  }

  return (
    <div className="min-h-screen bg-slate-50 pb-20 text-slate-900 md:pb-8">
      <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur">
        <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6">
          <div className="flex items-center gap-3"><button aria-label="Open navigation" onClick={() => setDrawer(true)} className="rounded-lg p-2 hover:bg-slate-100"><Menu className="h-5 w-5" /></button><div className="flex items-center gap-2 text-lg font-bold tracking-tight"><span className="flex h-8 w-8 items-center justify-center rounded-xl bg-emerald-100 text-emerald-700"><Leaf className="h-4 w-4" /></span>BinWise <span className="text-emerald-600">AI</span></div></div>
          <div className="relative flex items-center gap-2">
            <div className="hidden items-center gap-2 rounded-full border border-amber-200 bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-700 md:flex"><Zap className="h-3.5 w-3.5 fill-amber-500 text-amber-500" />{userXp} XP</div>
            {loggedIn ? <><button aria-label="Open profile menu" onClick={() => setProfileOpen((value) => !value)} className="flex h-9 w-9 items-center justify-center rounded-full bg-emerald-600 text-sm font-bold text-white shadow-sm ring-2 ring-emerald-100 hover:bg-emerald-700">{userName.split(' ').map((part) => part[0]).join('')}</button>{profileOpen && <div className="absolute right-0 top-12 z-40 w-72 rounded-2xl border border-slate-200 bg-white p-4 shadow-xl"><div className="flex items-center gap-3 border-b border-slate-100 pb-4"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-emerald-100 font-bold text-emerald-700">{userName.split(' ').map((part) => part[0]).join('')}</div><div className="min-w-0"><p className="truncate text-sm font-bold text-slate-900">{userName}</p><p className="truncate text-xs text-slate-500">{userEmail}</p></div></div><div className="my-3 flex items-center justify-between rounded-xl bg-emerald-50 px-3 py-2 text-xs"><span className="font-semibold text-emerald-800">Personal Karma XP</span><span className="font-bold text-emerald-700">{userXp} XP</span></div><button onClick={() => { setLoggedIn(false); setProfileOpen(false) }} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold text-slate-600 hover:bg-slate-50"><LogOut className="h-4 w-4" />Sign Out</button></div>}</> : <button onClick={() => { setAuthOpen(true); setAuthError('') }} className="hidden rounded-xl bg-emerald-600 px-4 py-2 text-sm font-bold text-white shadow-sm hover:bg-emerald-700 md:block">Sign In</button>}
          </div>
        </div>
      </header>

      {drawer && <><button aria-label="Close navigation" className="fixed inset-0 z-40 bg-slate-900/20" onClick={() => setDrawer(false)} /><aside data-navigation-drawer className="fixed inset-y-0 left-0 z-50 w-80 max-w-[88vw] overflow-y-auto [scrollbar-width:thin] bg-white p-6 shadow-2xl"><div className="mb-10 flex items-center justify-between"><div className="flex items-center gap-2 font-bold"><span className="flex h-8 w-8 items-center justify-center rounded-xl bg-emerald-100 text-emerald-700"><Leaf className="h-4 w-4" /></span>BinWise AI</div><button onClick={() => setDrawer(false)} className="rounded-lg p-2 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><p className="mb-3 text-xs font-bold uppercase tracking-widest text-slate-400">Navigate</p><div className="mb-8 space-y-1">{[['home', 'Home', Leaf], ['leaderboard', 'Leaderboard', Trophy], ['impact', 'Community Impact', GlobeIcon]].map(([tab, label, Icon]) => <button key={tab as string} onClick={() => { setActiveTab(tab as 'home' | 'leaderboard' | 'impact'); setDrawer(false) }} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm font-semibold transition ${activeTab === tab ? 'bg-emerald-50 text-emerald-600' : 'text-slate-500 hover:bg-slate-50'}`}><Icon className="h-4 w-4" />{label}</button>)}</div><p className="mb-3 text-xs font-bold uppercase tracking-widest text-slate-400">Data sources</p><div className="mb-8 space-y-2"><a href="https://cpcb.nic.in/" target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border border-emerald-100 bg-emerald-50 p-3 text-sm transition hover:bg-emerald-100"><span><span className="block font-semibold text-emerald-900">CPCB</span><span className="mt-1 block text-xs leading-5 text-emerald-700">Waste rules and pollution guidance</span></span><ExternalLink className="h-4 w-4 shrink-0 text-emerald-600" /></a><a href="https://moef.gov.in/" target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm transition hover:bg-slate-100"><span><span className="block font-semibold text-slate-800">MoEFCC</span><span className="mt-1 block text-xs leading-5 text-slate-500">Environment policies and regulations</span></span><ExternalLink className="h-4 w-4 shrink-0 text-slate-500" /></a><a href="https://swachhbharatmission.gov.in/" target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm transition hover:bg-slate-100"><span><span className="block font-semibold text-slate-800">Swachh Bharat Mission</span><span className="mt-1 block text-xs leading-5 text-slate-500">Solid waste management resources</span></span><ExternalLink className="h-4 w-4 shrink-0 text-slate-500" /></a></div><p className="mb-3 text-xs font-bold uppercase tracking-widest text-slate-400">About</p><div className="space-y-2 text-sm text-slate-600"><button className="flex w-full items-center gap-3 rounded-lg p-3 text-left hover:bg-slate-50"><Sparkles className="h-4 w-4 text-emerald-600" />Tech stack</button><button className="flex w-full items-center gap-3 rounded-lg p-3 text-left hover:bg-slate-50"><CircleHelp className="h-4 w-4 text-emerald-600" />FAQs</button></div><div className="mt-10 rounded-xl bg-slate-50 p-4"><p className="mb-3 text-xs font-bold uppercase tracking-widest text-slate-400">Phase 2 roadmap</p><p className="text-sm text-slate-600">Daily streaks · Personal waste analytics · Audio voice advice</p></div></aside></>}

      <main className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:py-8"><div className="mb-7 flex flex-col items-center text-center"><p className="mb-2 text-sm font-semibold text-emerald-600">Your responsible waste companion</p><h1 className="text-balance text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">Sort smarter. <span className="text-emerald-600">Live cleaner.</span></h1><p className="mt-2 max-w-xl text-center text-sm leading-6 text-slate-500">Identify any household item and get CPCB-guided advice for what happens next.</p></div>
        {activeTab === 'home' && <div className="grid gap-6 lg:grid-cols-12">
          <div className="space-y-6 lg:col-span-7">
            <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-center justify-between"><div><h2 className="text-lg font-bold">Hybrid waste scanner</h2><p className="mt-1 text-sm text-slate-500">Use a photo, a description, or both.</p></div><div className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">AI ready</div></div><div className="grid gap-3 sm:grid-cols-2"><button onClick={() => fileRef.current?.click()} className="group flex min-h-36 flex-col items-center justify-center rounded-xl border-2 border-dashed border-slate-200 bg-slate-50 p-4 transition hover:border-emerald-400 hover:bg-emerald-50"><input ref={fileRef} type="file" accept="image/*" className="hidden" onChange={(e) => setFileName(e.target.files?.[0]?.name ?? '')} />{fileName ? <><PackageCheck className="mb-2 h-8 w-8 text-emerald-600" /><span className="max-w-full truncate text-sm font-semibold">{fileName}</span><span className="mt-1 text-xs text-slate-500">Ready to scan</span></> : <><div className="mb-2 flex gap-2"><CloudUpload className="h-7 w-7 text-emerald-600" /><Camera className="h-7 w-7 text-slate-400" /></div><span className="text-sm font-semibold">Drop image or use camera</span><span className="mt-1 text-xs text-slate-500">JPG, PNG up to 10MB</span></>}</button><label className="group flex min-h-36 cursor-text flex-col rounded-xl border border-slate-200 bg-white p-4 transition hover:border-emerald-400 hover:bg-emerald-50 focus-within:border-emerald-500 focus-within:ring-2 focus-within:ring-emerald-100"><span className="mb-2 flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-slate-400"><Search className="h-3.5 w-3.5" /> Describe item</span><textarea value={input} onChange={(e) => setInput(e.target.value)} placeholder="Describe waste item or material condition..." className="min-h-20 flex-1 resize-none bg-transparent text-sm outline-none placeholder:text-slate-400" /></label></div><button disabled={analyzing || (!input.trim() && !fileName)} onClick={analyze} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-emerald-600 py-3.5 text-sm font-bold text-white shadow-sm transition hover:bg-emerald-700 disabled:cursor-not-allowed disabled:opacity-50">{analyzing ? <><span className="h-4 w-4 animate-spin rounded-full border-2 border-white/40 border-t-white" />Analyzing waste...</> : <><Sparkles className="h-4 w-4" />{analyzed ? 'Waste analyzed' : 'Analyze waste'} <ArrowRight className="h-4 w-4" /></>}</button><div className="mt-5 flex flex-wrap gap-2"><span className="w-full text-xs font-semibold text-slate-400">Try a sample</span>{samples.map((sample) => <button key={sample.key} onClick={() => { setInput(sample.label); setResult(sample.key) }} className="flex items-center gap-1.5 rounded-full border border-slate-200 px-3 py-1.5 text-xs font-medium text-slate-600 hover:border-emerald-300 hover:text-emerald-700"><sample.icon className="h-3.5 w-3.5" />{sample.label}</button>)}</div></section>

            <section id="ai-classification-result" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><SectionTitle icon={ResultIcon}>AI classification result</SectionTitle><div className="flex items-start justify-between gap-4"><div><div className={`inline-flex items-center gap-2 rounded-full px-3 py-1.5 text-xs font-bold ${item.chip}`}><ResultIcon className="h-3.5 w-3.5" />{item.short}</div><h2 className="mt-3 text-2xl font-bold tracking-tight">{item.material}</h2><p className="mt-1 text-sm text-slate-500">{item.label}</p></div><div className="text-right"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Confidence</p><p className="mt-1 text-2xl font-bold text-emerald-600">98%</p></div></div><div className="mt-7"><div className="mb-3 flex items-center justify-between text-xs font-semibold"><span>Environmental harm meter</span><span className="text-slate-500">{item.riskLabel}</span></div><div aria-label={`Harm risk level ${item.risk} of 4`} role="progressbar" aria-valuemin={1} aria-valuemax={4} aria-valuenow={item.risk} className="flex h-3 gap-1 overflow-hidden rounded-full bg-slate-100"><div className={`w-1/4 rounded-l-full transition-all ${item.risk === 1 ? 'bg-emerald-500 opacity-100 shadow-[0_0_8px_rgba(16,185,129,0.45)]' : 'bg-emerald-500 opacity-40'}`} /><div className={`w-1/4 transition-all ${item.risk === 2 ? 'bg-yellow-400 opacity-100 shadow-[0_0_8px_rgba(250,204,21,0.45)]' : item.risk > 2 ? 'bg-yellow-400 opacity-40' : 'bg-slate-200 opacity-20'}`} /><div className={`w-1/4 transition-all ${item.risk === 3 ? 'bg-orange-500 opacity-100 shadow-[0_0_8px_rgba(249,115,22,0.45)]' : item.risk > 3 ? 'bg-orange-500 opacity-40' : 'bg-slate-200 opacity-20'}`} /><div className={`w-1/4 rounded-r-full transition-all ${item.risk === 4 ? 'bg-red-500 opacity-100 shadow-[0_0_8px_rgba(239,68,68,0.45)]' : 'bg-slate-200 opacity-20'}`} /></div><div className="mt-2 flex justify-between text-[10px] font-bold uppercase tracking-wider"><span className={item.risk === 1 ? 'font-extrabold text-emerald-600' : 'font-bold text-emerald-600 opacity-40'}>Safe</span><span className={item.risk === 2 ? 'font-extrabold text-yellow-600' : item.risk > 2 ? 'font-bold text-yellow-600 opacity-40' : 'text-slate-400 opacity-20'}>Moderate</span><span className={item.risk === 3 ? 'font-extrabold text-orange-600' : item.risk > 3 ? 'font-bold text-orange-600 opacity-40' : 'text-slate-400 opacity-20'}>High</span><span className={item.risk === 4 ? 'font-extrabold text-red-600' : 'text-slate-400 opacity-20'}>Critical</span></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className={`rounded-xl p-4 transition-colors ${harm.card}`}><p className={`mb-2 text-xs font-bold uppercase tracking-wider ${harm.label}`}>{result === 'green' ? 'Current harm fact: None' : 'Current harm fact'}</p><p className={`text-sm leading-6 ${harm.body}`}>{item.current}</p></div><div className={`rounded-xl p-4 transition-colors ${harm.card}`}><p className={`mb-2 text-xs font-bold uppercase tracking-wider ${harm.label}`}>{result === 'green' ? 'Future disposal risk: Precaution' : 'Future disposal risk'}</p><p className={`text-sm leading-6 ${harm.body}`}>{item.future}</p></div></div></section>

            <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><SectionTitle icon={Recycle}>Resource recovery & DIY guide</SectionTitle><div className="grid gap-3 sm:grid-cols-2"><div className="border-l-2 border-emerald-500 pl-4"><button onClick={() => setGuide('industrial')} className="text-left text-sm font-bold uppercase tracking-wider text-emerald-700 underline decoration-emerald-300 underline-offset-4 hover:text-emerald-800">Industrial process</button><p className="mt-2 text-sm font-semibold leading-6 text-slate-800">{item.process}</p></div><div className="border-l-2 border-slate-300 pl-4"><button onClick={() => setGuide('diy')} className="text-left text-sm font-bold uppercase tracking-wider text-emerald-700 underline decoration-emerald-300 underline-offset-4 hover:text-emerald-800">Your DIY tip</button><p className="mt-2 text-sm font-semibold leading-6 text-slate-800">{item.tip}</p></div></div></section>
          </div>

          <div className="space-y-6 lg:col-span-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-center justify-between"><SectionTitle icon={Footprints}>Your waste journey</SectionTitle><span className="text-xs font-semibold text-emerald-600">{journey + 1}/5 steps</span></div><div className="space-y-0">{['Scanned', 'DIY Prepared', 'Dropped at Center', 'In Transit (Est)', 'Recycled (Est)'].map((step, index) => { const done = index < journey && !(index === 1 && diySkipped); const current = index === journey; return <div key={step} className="flex gap-3"><div className="flex w-6 flex-col items-center"><div className={`z-10 flex h-6 w-6 items-center justify-center rounded-full border-2 ${done ? 'border-emerald-600 bg-emerald-600 text-white' : current ? 'border-emerald-600 bg-white text-emerald-600' : 'border-slate-200 bg-white text-slate-300'}`}>{done ? <Check className="h-3.5 w-3.5" /> : <span className="h-1.5 w-1.5 rounded-full bg-current" />}</div>{index < 4 && <div className={`w-0.5 flex-1 ${index < journey ? 'bg-emerald-500' : 'bg-slate-200'}`} />}</div><div className="pb-7 pt-0.5"><p className={`text-sm font-semibold ${done || current ? 'text-slate-800' : 'text-slate-400'}`}>{step}</p><p className="mt-1 text-xs text-slate-400">{done ? 'Complete' : current ? 'Next step' : index === 2 ? 'Pending' : 'Estimated'}</p></div></div>})}</div><a href="https://www.google.com/maps/search/waste+drop-off+center+near+me" target="_blank" rel="noreferrer" className="mb-3 flex w-full items-center justify-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700 transition hover:bg-emerald-100"><LocateFixed className="h-4 w-4" />Find nearest drop-off centre<ChevronRight className="h-4 w-4" /></a><label className="mt-1 flex w-full cursor-pointer items-center justify-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700 transition hover:bg-emerald-100"><input type="file" accept="image/*" className="sr-only" disabled={journey >= 4} onChange={(event) => { const file = event.target.files?.[0]; if (file) { setProofName(file.name); setUserXp(20); if (journey < 4) dropped() } }} /><CloudUpload className="h-4 w-4" />{journey >= 4 ? 'Journey complete' : proofName ? 'Proof uploaded' : 'Upload drop-off proof'}{journey < 4 && <ChevronRight className="h-4 w-4" />}</label></section>

          <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-4 flex items-center justify-between gap-3"><SectionTitle icon={Trophy}>Live leaderboard</SectionTitle><div className="flex items-center gap-2 rounded-full border border-amber-200 bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-700 md:hidden"><Zap className="h-3.5 w-3.5 fill-amber-500 text-amber-500" />{userXp} XP</div></div><div className="space-y-2">{[['1', 'EcoWarrior1', '520 XP'], ['2', 'GreenHero', '410 XP'], ['3', loggedIn ? userName : 'You', `${userXp} XP`]].map(([rank, name, xp]) => <div key={rank} className={`flex items-center gap-3 rounded-xl p-3 ${rank === '3' ? 'bg-emerald-50 ring-1 ring-emerald-100' : 'bg-slate-50'}`}><span className="flex h-7 w-7 items-center justify-center rounded-full bg-white text-xs font-bold text-slate-500">{rank}</span><span className="flex-1 text-sm font-semibold">{name}</span><span className="text-xs font-bold text-slate-500">{xp}</span></div>)}</div></section>
          <section className="rounded-2xl bg-emerald-600 p-6 text-white shadow-sm"><div className="flex items-center gap-2 text-emerald-100"><Leaf className="h-4 w-4" /><span className="text-xs font-bold uppercase tracking-[0.14em]">Global impact</span></div><p className="mt-4 text-4xl font-bold tracking-tight">15,240<span className="text-emerald-200">+</span></p><p className="mt-1 text-sm text-emerald-100">items sorted responsibly</p><div className="mt-5 flex items-center gap-2 text-xs text-emerald-100"><Flame className="h-4 w-4" />Keep the momentum going</div></section></div>
        </div>}
        {activeTab === 'leaderboard' && <section className="mx-auto max-w-4xl rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-8"><SectionTitle icon={Trophy}>Live Eco-Warriors Leaderboard</SectionTitle><div className="space-y-3">{[['1','EcoWarrior1','2,540 XP','Compost Captain'],['2','GreenHero','1,980 XP','Plastic Slayer'],['3',loggedIn ? userName : 'You',`${userXp} XP`,'BinWise Rookie'],['4','EarthKeeper','840 XP','Clean Streets']].map(([rank,name,xp,badge]) => <div key={rank} className={`flex items-center gap-1 rounded-2xl px-2 py-3 sm:gap-3 sm:p-4 ${rank === '3' ? 'bg-emerald-50 ring-1 ring-emerald-200' : 'bg-slate-50'}`}><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-white text-sm font-bold text-slate-500 sm:h-10 sm:w-10">{rank}</span><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-emerald-100 text-sm font-bold text-emerald-700 sm:h-10 sm:w-10">{String(name).slice(0,1).toUpperCase()}</span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-bold">{name}</span><span className="mt-1 inline-flex rounded-full bg-white px-2 py-0.5 text-[10px] font-bold text-emerald-700">{badge}</span></span><span className="shrink-0 whitespace-nowrap text-xs font-bold text-slate-600 sm:text-sm">{xp}</span></div>)}</div></section>}
        {activeTab === 'impact' && <section className="mx-auto max-w-4xl space-y-6"><div className="rounded-2xl bg-emerald-600 p-6 text-white shadow-sm sm:p-8"><p className="text-xs font-bold uppercase tracking-widest text-emerald-100">Global Community Stats</p><p className="mt-4 text-5xl font-bold">15,240+</p><p className="mt-2 text-sm text-emerald-100">items segregated responsibly by the BinWise community</p></div><div className="grid gap-4 sm:grid-cols-3">{[['CO₂ offset','4.8 t','Equivalent to 1,120 trees'],['Landfill space saved','12.6 m³','Material recovered'],['Active eco-warriors','2,840','Growing every day']].map(([label,value,detail]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-3 text-3xl font-bold text-emerald-600">{value}</p><p className="mt-2 text-xs leading-5 text-slate-500">{detail}</p></div>)}</div></section>}
      </main>

      <nav className="fixed inset-x-0 bottom-0 z-20 flex h-16 items-center justify-around border-t border-slate-200 bg-white px-8 md:hidden">{[['home','Home',Leaf],['leaderboard','Ranks',Trophy],['impact','Impact',GlobeIcon],['signin','Sign In',UserRound]].map(([tab,label,Icon]) => <button key={tab as string} onClick={() => tab === 'signin' ? setAuthOpen(true) : setActiveTab(tab as 'home' | 'leaderboard' | 'impact')} className={`relative flex flex-col items-center gap-1 px-3 py-1 text-xs font-semibold transition ${activeTab === tab ? 'text-emerald-600' : 'text-slate-400'}`}><Icon className="h-5 w-5" /><span>{label}</span>{activeTab === tab && <span className="absolute -top-2 h-1 w-8 rounded-full bg-emerald-500" />}</button>)}</nav>
      {toast && <div role="status" className="fixed bottom-20 left-1/2 z-50 flex -translate-x-1/2 items-center gap-2 rounded-full bg-slate-900 px-5 py-3 text-sm font-bold text-white shadow-xl md:bottom-8"><Zap className="h-4 w-4 fill-yellow-400 text-yellow-400" />+20 XP Earned!</div>}
      {guide && <div role="dialog" aria-modal="true" aria-labelledby="guide-title" className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/30 p-4" onClick={() => setGuide(null)}><div className="w-full max-w-lg rounded-2xl border border-slate-200 bg-white p-6 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-emerald-600">BinWise process guide</p><h2 id="guide-title" className="mt-1 text-xl font-bold">{guide === 'industrial' ? 'How the industrial process works' : 'How to do it at home'}</h2></div><button aria-label="Close process guide" onClick={() => setGuide(null)} className="rounded-lg p-2 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="mt-5 rounded-xl bg-emerald-50 p-4"><p className="text-sm font-semibold leading-6 text-emerald-950">{guide === 'industrial' ? item.process : item.tip}</p></div><div className="mt-5 space-y-3">{(guide === 'industrial' ? ['Waste is collected and separated at a certified facility.', 'The material is treated using the appropriate controlled process.', 'Recovered materials are converted into useful products or energy.'] : ['Prepare the item using the recommended safety steps.', 'Keep it separate from other household waste.', 'Take it to the correct bin or certified collection point.']).map((step, index) => <div key={step} className="flex gap-3"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-emerald-600 text-xs font-bold text-white">{index + 1}</span><p className="text-sm leading-6 text-slate-600">{step}</p></div>)}</div>{guide === 'diy' ? <label className="mt-6 flex w-full cursor-pointer items-center justify-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700 transition hover:bg-emerald-100"><input type="file" accept="image/*" className="sr-only" onChange={(event) => { const file = event.target.files?.[0]; if (file) { setDiyProofName(file.name); setDiySkipped(false) } }} /><CloudUpload className="h-4 w-4" />{diyProofName ? 'DIY prepared — proof uploaded' : 'Upload proof: DIY prepared'}</label> : <button onClick={() => setGuide(null)} className="mt-6 w-full rounded-xl bg-emerald-600 py-3 text-sm font-bold text-white hover:bg-emerald-700">Done</button>}{guide === 'diy' && <button onClick={() => { setGuide(null); setDiySkipped(true); setJourney(2) }} className="mt-3 w-full rounded-xl border border-slate-200 py-3 text-sm font-bold text-slate-600 hover:bg-slate-50">Continue without DIY</button>}</div></div>}
      {dialog && <div role="dialog" aria-modal="true" aria-labelledby="dialog-title" className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/30 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-red-600">Certified collection</p><h2 id="dialog-title" className="mt-1 text-xl font-bold">Nearby drop-off hubs</h2></div><button aria-label="Close dialog" onClick={() => setDialog(false)} className="rounded-lg p-2 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="mt-5 space-y-3">{[['GreenCycle Hub', '1.2 km · Open today'], ['EcoSafe Collection Center', '2.8 km · Closes 6:00 PM'], ['CPCB Partner Point', '4.1 km · Open today']].map(([name, details]) => <button key={name} onClick={() => setDialog(false)} className="flex w-full items-center gap-3 rounded-xl border border-slate-200 p-4 text-left hover:border-emerald-300"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-emerald-50 text-emerald-600"><LocateFixed className="h-4 w-4" /></span><span className="flex-1"><span className="block text-sm font-bold">{name}</span><span className="mt-1 block text-xs text-slate-500">{details}</span></span><ArrowRight className="h-4 w-4 text-slate-400" /></button>)}</div></div></div>}
      {authOpen && <div role="dialog" aria-modal="true" aria-labelledby="auth-title" className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/30 p-4"><div className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-emerald-600">Welcome to BinWise</p><h2 id="auth-title" className="mt-1 text-2xl font-bold">Your cleaner journey starts here</h2></div><button aria-label="Close authentication dialog" onClick={() => setAuthOpen(false)} className="rounded-lg p-2 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="mt-6 grid grid-cols-2 rounded-xl bg-slate-100 p-1"><button onClick={() => setAuthTab('login')} className={`rounded-lg py-2 text-sm font-bold ${authTab === 'login' ? 'bg-white text-emerald-700 shadow-sm' : 'text-slate-500'}`}>Log In</button><button onClick={() => setAuthTab('signup')} className={`rounded-lg py-2 text-sm font-bold ${authTab === 'signup' ? 'bg-white text-emerald-700 shadow-sm' : 'text-slate-500'}`}>Sign Up</button></div><form className="mt-6 space-y-4" onSubmit={(event) => { event.preventDefault(); const data = new FormData(event.currentTarget); const email = String(data.get('email') || '').trim(); if (!email || !String(data.get('password') || '')) { setAuthError('Enter an email and password to continue.'); return }; setUserEmail(email); setUserName(authTab === 'signup' ? 'New Eco Champion' : email.split('@')[0]); setLoggedIn(true); setAuthOpen(false); setAuthError('') }}><label className="block text-sm font-semibold">Email Address<div className="mt-2 flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2.5 focus-within:border-emerald-500"><Mail className="h-4 w-4 text-slate-400" /><input name="email" type="email" placeholder="alex@example.com" className="min-w-0 flex-1 bg-transparent text-sm outline-none" /></div></label><label className="block text-sm font-semibold">Password<div className="mt-2 flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2.5 focus-within:border-emerald-500"><LockKeyhole className="h-4 w-4 text-slate-400" /><input name="password" type="password" placeholder="••••••••" className="min-w-0 flex-1 bg-transparent text-sm outline-none" /></div></label>{authError && <p className="text-xs font-semibold text-red-600">{authError}</p>}<button type="submit" className="w-full rounded-xl bg-emerald-600 py-3 text-sm font-bold text-white hover:bg-emerald-700">{authTab === 'login' ? 'Log In' : 'Create Account'}</button></form><div className="my-5 flex items-center gap-3 text-xs text-slate-400"><span className="h-px flex-1 bg-slate-200" />or<span className="h-px flex-1 bg-slate-200" /></div><div className="grid gap-2 sm:grid-cols-2"><button type="button" onClick={() => setAuthError('Magic link sent to your inbox.')} className="flex items-center justify-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 py-2.5 text-xs font-bold text-emerald-700 hover:bg-emerald-100"><Mail className="h-4 w-4" />Send OTP / Magic Link</button><button type="button" onClick={() => setAuthError('Google sign-in is ready for the next release.')} className="flex items-center justify-center gap-2 rounded-xl border border-slate-200 py-2.5 text-xs font-bold text-slate-600 hover:bg-slate-50"><span className="font-bold text-base">G</span>Continue with Google</button></div></div></div>}
    </div>
  )
}

function GlobeIcon() { return <span className="flex h-5 w-5 items-center justify-center rounded-full border-2 border-current text-[9px]">◎</span> }
